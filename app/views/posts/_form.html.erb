<%= form_for(:post, url: posts_url) do |f| %>
  <div class="form-group">
    <%= f.text_area :content, class: "form-control", placeholder: "What's on your mind", rows: '7' %>
  </div>
  <%= f.submit 'Post', class: "btn btn-primary btn-lg pull-right" %>
<% end %>